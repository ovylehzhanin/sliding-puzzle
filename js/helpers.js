/*
 * Helpers-------------------------
*/
function _qs(selector, scope) {
  return (scope || document).querySelector(selector);
}

function _qsa(selector, scope) {
  return (scope || document).querySelector(selector);
}
/*---------------------------------*/

